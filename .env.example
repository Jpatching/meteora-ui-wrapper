# Meteora Invent Path
# Point this to your local meteora-invent repository
METEORA_INVENT_PATH=/path/to/meteora-invent

# Network Configuration
# Default network: localnet | devnet | mainnet-beta
NEXT_PUBLIC_DEFAULT_NETWORK=devnet

# RPC Endpoints (optional - uses Solana defaults if not specified)
NEXT_PUBLIC_LOCALNET_RPC=http://localhost:8899
NEXT_PUBLIC_DEVNET_RPC=https://api.devnet.solana.com
NEXT_PUBLIC_MAINNET_RPC=https://api.mainnet-beta.solana.com

# Lighthouse API Key (required for metadata builder)
# Get your API key at https://lighthouse.storage
# "Pay once, store forever" model - perfect for permanent token metadata
# Free tier: 100MB, then ~$0.0017/GB for permanent storage
LIGHTHOUSE_API_KEY=your_lighthouse_api_key_here

# Backend API URL
# Points to the backend server for pool data and token CA searches
# Development: http://localhost:4000
# Production: Set to your Railway backend URL (e.g., https://your-backend.railway.app)
NEXT_PUBLIC_BACKEND_URL=http://localhost:4000
